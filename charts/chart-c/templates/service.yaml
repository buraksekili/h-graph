apiVersion: v1
kind: Service
metadata:
  name: {{ include "chart-c.fullname" . }}
  labels:
    {{- include "chart-c.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chart-c.selectorLabels" . | nindent 4 }}
